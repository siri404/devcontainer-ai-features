{
    "name": "Gemini CLI",
    "id": "gemini-cli",
    "version": "1.0.0",
    "description": "Installs the Google Gemini CLI tool for interacting with Google's Gemini AI models",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of gemini-cli to install"
        },
        "apiKey": {
            "type": "string",
            "default": "",
            "description": "Optional: Google API key for Gemini (can also be set via environment variable GEMINI_API_KEY)"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}

